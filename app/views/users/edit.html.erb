<% content_for  :title do  -%>
  <%= t "users.edit_title" %>
<% end -%>

<div id="user_container">
  <%= render :partial => 'form', :locals => {:user => @user, :action => user_path(@user), :submit_text => t("shared.save") } %>
</div>
